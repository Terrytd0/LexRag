"""Metric wrappers: recall@K/precision@K (retrieval.py), RAGAS faithfulness/context/
relevancy plus a DeepEval threshold wrapper (generation.py), refusal accuracy
(refusal.py), and a hand-rolled Pearson correlation (correlation.py) used by the
one-off `scripts/confidence_correlation.py` analysis."""
